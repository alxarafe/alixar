meta {
  name: 06-verify-deletion
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}{{apiBase}}/supplierorders/{{testSupplierOrderId}}
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("El pedido de proveedor eliminado no existe", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
