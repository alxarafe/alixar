meta {
  name: API Status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}{{apiBase}}/status
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
  Accept: application/json
}

tests {
  test("Status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
