meta {
  name: "List Members"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/members"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
