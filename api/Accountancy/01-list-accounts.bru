meta {
  name: "List Accounting Accounts"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/accountancy/accounts"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
