meta {
  name: "List Expense Reports"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/expensereports"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
