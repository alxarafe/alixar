meta {
  name: "List EmailTemplates"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/emailtemplates"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
