meta {
  name: 05-delete-supplierinvoice
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}{{apiBase}}/supplierinvoices/{{testSupplierInvoiceId}}
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("Delete exitoso", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
