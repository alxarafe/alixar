meta {
  name: "List Salaries"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/salaries"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
