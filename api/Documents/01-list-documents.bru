meta {
  name: "List Documents"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/documents"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isDefined
}
