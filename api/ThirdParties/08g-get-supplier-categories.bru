meta {
  name: 08g-get-supplier-categories
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}{{apiBase}}/thirdparties/{{testThirdPartyId}}/supplier_categories
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("Status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Respuesta es un array", function() {
    expect(res.getBody()).to.be.an('array');
  });
}
