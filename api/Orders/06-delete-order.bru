meta {
  name: 06-delete-order
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}{{apiBase}}/orders/{{testOrderId}}
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("Delete exitoso", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
