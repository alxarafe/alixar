meta {
  name: 07-verify-deletion
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}{{apiBase}}/orders/{{testOrderId}}
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("El pedido eliminado no existe", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
