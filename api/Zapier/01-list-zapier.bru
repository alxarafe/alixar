meta {
  name: "List Zapier"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/zapier"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
