meta {
  name: 09 - Borrar Factura
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}{{apiBase}}/invoices/{{testInvoiceId}}
  body: none
  auth: none
}

headers {
  DOLAPIKEY: {{apiKey}}
}

tests {
  test("Status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
