meta {
  name: "List KnowledgeManagement"
  type: "http"
  seq: 1
}

get {
  url: "{{baseUrl}}/api/knowledgemanagement"
  body: "none"
  auth: "none"
}

headers {
  DOLAPIKEY: "{{apiKey}}"
}

assert {
  res.status: eq 200
  res.body: isArray
}
