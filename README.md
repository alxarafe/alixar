# Alixar ERP

Alixar is a rewrite of Dolibarr using the Alxarafe Framework.

## Requirements

- PHP 8.5+
- Composer
- Docker (optional)

## Installation

1. Clone the repository.
2. Run `composer install`.
3. Configure your `.env` file.
4. Run migrations using `./bin/run_migrations.sh`.
